# MCP Server Framework Configuration

# Main MCP Server Port
MCP_SERVER_PORT=3200

# Map API Provider (google, osm, or riskscape)
# Default: google (backward compatibility)
# Options:
#   - google: Google Maps (requires GOOGLE_MAPS_API_KEY)
#   - osm/openstreetmap: OpenStreetMap (no API key required)
#   - riskscape: RiskScape API (requires RISKSCAPE_API_KEY)
MAP_API_PROVIDER=riskscape

# Google Maps API Key (required only when MAP_API_PROVIDER=google)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# RiskScape API Key (required only when MAP_API_PROVIDER=riskscape)
RISKSCAPE_API_KEY=your_riskscape_api_key_here

# Add your custom server ports here
# YOUR_CUSTOM_SERVER_PORT=3001